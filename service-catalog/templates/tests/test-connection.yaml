apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "service-catalog.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "service-catalog.name" . }}
    helm.sh/chart: {{ include "service-catalog.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "service-catalog.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
